# Evaluation — the friction/observation log

> The **primary evidence source** for changing CANVAS. Append-only: never rewrite history, only add.

Encode into the system on **recurrence**, not first sight:
- **First friction** → fix it in the produced app + log it here.
- **Second/third recurrence** → it earns a candidate in [docs/initiatives/](../initiatives/), then an action item once proven by real need.

Each entry: date · system version · what was attempted · what happened · evidence (file:line) · recurrence count · disposition.

## Entries

*(none yet — CANVAS is at Phase 0/1, not yet dogfooded. First real friction is expected when golden-task GT-A1 runs.)*
